{
  "name": "thingsource",
  "version": "1.0.0",
  "description": "Autonomous agent that researches interesting things and blogs about them using Gemini Flash with Google Search Grounding.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "agent": "node agent.js",
    "run-and-push": "node agent.js && git add public/posts.json public/images/ && git commit -m \"chore: auto-publish new discovery\" && git push",
    "build": "node build.js",
    "generate-social-card": "node scripts/generate-social-card.js"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@netlify/blobs": "^8.0.0",
    "@netlify/functions": "^2.0.0",
    "resend": "^4.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "puppeteer": "^21.0.0"
  }
}
